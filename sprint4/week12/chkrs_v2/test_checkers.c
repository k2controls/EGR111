#include "checkers.h"

#if TESTING
#include "acutest.h"

void can_make_sample_test_pass(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}

void can_make_checker_string(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}

void can_make_king_checker_string(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}

void can_check_for_valid_cell_pointer(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}
void can_check_if_checker_cell(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}
void can_check_if_black_cell(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}
void can_check_if_red_cell(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}
void can_check_if_king_cell(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}
void can_check_if_empty_cell(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}
void can_check_if_no_play_cell(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}
void can_check_if_foe_cell(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}
void can_check_if_valid_checker_number(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}
void can_check_if_checker_is_on_board(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}
void can_get_cell_owner(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}
void can_locate_cell(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}
void can_get_next_cell_pointer(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}
void can_check_board_for_jump(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}
void can_check_checker_for_jump(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}
void can_check_checker_for_move(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}
void can_check_game_over(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}
void can_get_next_player(void)
{
    //arrange
    bool test = false;
    //act
    test = true;
    //assert
    TEST_CHECK(test);
}

// List of tests - key, value pairs with {NULL, NULL} termination.
TEST_LIST = {
    { "can make sample test pass ", can_make_sample_test_pass },
    { "can make checker string ", can_make_checker_string },
    { "can make king checker string ", can_make_king_checker_string },

    // { "can check for valid cell pointer", can_check_for_valid_cell_pointer },
    // { "can check if checker cell", can_check_if_checker_cell },
    // { "can check if black cell", can_check_if_black_cell },
    // { "can check if red cell", can_check_if_red_cell },
    // { "can check if king cell", can_check_if_king_cell },
    // { "can check if empty cell", can_check_if_empty_cell },
    // { "can check if no play cell", can_check_if_no_play_cell },
    // { "can check if foe cell", can_check_if_foe_cell },
    // { "can check if valid checker number", can_check_if_valid_checker_number },
    // { "can check if checker is on board", can_check_if_checker_is_on_board },
    // { "can get cell owner", can_get_cell_owner },
    // { "can locate cell", can_locate_cell },
    // { "can get next cell pointer", can_get_next_cell_pointer },
    // { "can check board for jump", can_check_board_for_jump },
    // { "can check checker for jump", can_check_checker_for_jump },
    // { "can check checker for move", can_check_checker_for_move },
    // { "can check game over", can_check_game_over },
    // { "can get next player", can_get_next_player },

    { NULL, NULL }
};

#endif






























